# SQL Server names shown in the compare dropdowns (not IP addresses).
# Comma-separated names are merged with config/servers.json.
SQL_SERVER_NAMES=SQL-PROD-01,SQL-UAT-01
SQL_DATABASE=YourDatabase
SQL_USERNAME=
SQL_PASSWORD=
SQL_PORT=1433
SQL_SCHEMA=dbo
